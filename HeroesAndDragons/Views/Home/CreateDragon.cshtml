@model HeroesAndDragons.Models.Dragon
@{
    ViewData["Title"] = "Create a dragon";
}

<h3>Create a dragon</h3>

<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateDragon" method="post">
            @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>*@
            <div class="form-group">
                <input type="submit" value="Create Dragon!" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}